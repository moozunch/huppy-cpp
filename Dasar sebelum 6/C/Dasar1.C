#include <stdio.h>
// Library, kalau di C++ Include Iostream for STD Library

// Main is a function name that are integrated with C and C++ to marking the start of every program
int main() {
   // printf() displays the string inside quotation
   printf("Hello, World!"); //Very same like pascal
   return 0; // Is a must
}
// Kalau name extention pakai lowercase c dia bakal pakai actual c compiler tapi perlu library nya juga yan tidak ada path nya karena actual c compiler belum terinstall
// Kalau name extension pakai Uppercase C dia bakal terhubung ke library C++ yang dimana library dan path nya sudah terinstall dan ada
